1、相机管理界面的中间分割栏默认在最右侧，以右侧相机控制的最小宽度为准，
2、将运行看板中的看板统计中的OK数量，NG数量，今日统计，本周统计，本月统计，OK完成率可放在显示数字的左侧，从而降低该栏的高度
3、在运行看板界面中，将复位故障，跳过当前步骤，和当前模板，刷新看板，收起完整看板状态放在一行
4、将完整看板状态中的内容放到底部状态栏中，然后取消这个完整看板状态栏
5、刘晨给编辑中的检测流程配置部分，每列的列宽尽量可调整，最小宽度可调整成自适应
6、流程编辑中的产品模板/模型标签 模块和ROI配置区中间添加一个可拖动的分割栏，默认位置显示的中间位置，另外在检测流程配置部分也添加一个上下的可移动的分割栏
7、硬件配置中的IO控制，将输入输出的显示区域缩小，缩小到适应的最小宽度和高度即可，不用完全充面画面，
8、软件激活界面可显示到期时间