1、将运行看板的OK。NG数量那一行的显示信息放在看板状态里，节省界面空间，这样可以将图像显示区以及流程步骤状态显示区的占比变得更大一些，给客户更直观的感受
2、将最顶部的操作提示放在顶部按钮右侧，帮助的左侧，节省空间，
3、流程步骤最下方添加两个按钮，用来复位故障，以及跳过当前步骤，跳到下一步，跳到下一步时可弹窗显示选择当前步时OK通过还是NG通过，
4、底部状态栏的各个状态更新部分的代码给我添加相机的注释，
5、看板界面的流程步骤状态为每一步添加一个缩略图显示，用来显示当前步执行完成后的最后一帧图像，
6、相机管理界面将ROI设置区取消，添加相机打开，枚举，驱动选择，选择USB相机还是GIGE相机等，只用来控制相机，以及调整相机相关参数
7、将相机管理界面的ROI区域调整到流程编辑中，可放在左上角区域，产品模板/配方的左侧，当鼠标点击放大的时候，可弹窗显示大图，并且可进行ROI的创建和编辑，这部分你可结合现有界面功能，根据你的经验去添加
，因为单个界面可能放不下那么多的模块，所以可采用弹窗的方式。
8、硬件配置界面的串口设置将参数设置成横向排列
9、结果查询界面的查询，重置，导出按钮，可与查询条件横向排列，调整成单行显示，
10、参数界面，将相机参数删除，检测参数也删除，检测参数在流程中进行设置，保留系统参数
11、将各个界面的参数保存到config目录下，对应各自的名称，创建成yaml格式文件
12、将流程编辑保存到flow目录下，并且以每个模板名称在这个目录下创建对应的文件夹，这个模板名称给对应文件夹名称，作为查询的顶级索引，然后将对应模板的流程，ROI配置，标签配置，模型目录等内容信息，保存到这个文件夹下
13、修改内容后，如果修改了界面UI，记得把oriui以及ui目录下的对应文件也进行修改。
14、修改代码记得添加注释，主要注释这部分是干嘛用的，后期扩展应该怎么添加，方便我后期进行拓展