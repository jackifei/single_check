1、将左侧上面的相机管理名称和内容显示取消，不再显示某个页面名称，而是显示每个界面的对应检测结果，以及操作提示等内容，
2、流程编辑界面的加载标签左侧再添加要给加载模型按钮，用来加载模型，并且现在右侧添加一个对应标签的输入框，支持输入中文名称，用来对应英文的标签名称，可将此界面向左收缩到左侧隐藏
3、流程编辑界面最上面添加一个模板创建和编辑删除等功能区域，用来创建顶层模板，每个顶层模板对应着一个模型和流程配置，这样方便切换不同的产品配方
4、增加结果查询界面，用来查询每次检测的结果，流程是否检测完，有哪些对应提示和报警信息，可支持时间筛选，你可以对此界面在你的经验上进行丰富，
5、将设备通讯修改为硬件配置，建个里面的串口配置修改为选择串口，打开，或者关闭串口，参数以后可在代码中设定为固定值，不再更改，并且添加16个指示灯，16个开关按钮，用来控制IO卡的输入输出等功能，IO的通讯为ModbusTCP
6、增加MES对接界面，用来对接MES，选择哪些报文内容发送给MES，或者预留定制部分
7、右侧按钮栏调整到顶部水平栏，
9、每个界面的效果是否可单独运行，方便调试
10、给代码的各个部分进行注释，不用太详细，最好可以标注明白哪里可以插入功能，哪里可以替换模块等
11、流程编辑中添加置信度输入，检测数量，是否使用手势，如果使用手势，手势姿势名称可以进行选择
12、增加帮助按钮，在界面的边角处，点击可进行帮助内容查询，以及激活等操作
13、是否可以将每个page界面生成对应的一份.ui文件，我可以通过qtdesigner打开进行二次编辑，