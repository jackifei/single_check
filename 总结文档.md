# SOP 视觉检测软件框架 —— 项目总结文档

> 本文档用于快速建立对项目的整体认知，并作为后续升级、修改、扩展的参考底稿。
> 当前项目工作目录：`E:\codex_pj\single_check`

---

## 1. 项目概览

本项目是一个基于 **Python 3.12 + PyQt6** 的工业视觉检测软件 UI/交互框架 Demo，应用场景包括：

- 相机管理（USB / GIGE）
- 检测流程编辑（产品模板、模型、标签、ROI、检测步骤）
- 硬件配置（串口 + ModbusTCP IO 控制）
- 运行看板（实时统计、流程步骤状态）
- 结果查询（历史检测记录筛选）
- MES 对接（报文选择与自定义模板）
- 日志、系统参数、人员管理
- 软件授权与机器绑定

主窗口标题为：**“秦皇岛文视科技有限公司”**，程序内部名称为 **SOP / SOP Demo**。

当前阶段定位：**UI 与交互框架已经搭建完成，大部分业务逻辑为演示/占位实现**；USB 相机采集是已经接通的真实逻辑，而 GIGE 相机、ModbusTCP、MES、数据库、检测算法、流程引擎等仍以 demo 或“插入点”形式预留。

---

## 2. 技术栈与运行环境

| 项目 | 内容 |
| --- | --- |
| 语言 | Python 3.12 |
| GUI 框架 | PyQt6 >= 6.6 |
| 配置 | PyYAML >= 6.0 |
| 视觉采集 | opencv-python >= 4.8 |
| 打包 | PyInstaller |
| 平台 | Windows（主要） |

依赖文件：`requirements.txt`

```text
PyQt6>=6.6.0
PyYAML>=6.0
opencv-python>=4.8
```

运行方式：

```powershell
python main.py
```

独立调试某个页面（示例）：

```powershell
python app/pages/camera_page.py
python app/pages/run_dashboard_page.py
```

---

## 3. 目录结构与职责

```text
single_check/
├─ main.py                       # 程序入口
├─ requirements.txt              # 依赖
├─ README.md                     # 项目说明
├─ PROJECT_ARCHITECTURE.md       # 架构与扩展说明
├─ 打包注释.md                    # PyInstaller 打包参数说明
├─ 总结文档.md                    # 本文档
├─ main.spec / SOP.spec / SOP_APP.spec  # 打包配置
├─ MyAppLog.ico                  # 应用图标
├─ license_data.json             # 授权数据（已激活示例）
├─ license_tool.py               # 授权码生成工具（GUI）
├─ config/                       # 页面参数 YAML 配置
├─ flow/                         # 流程模板配置（按模板名分目录）
├─ algorithms/                   # 检测算法（预留，暂无实现）
├─ drivers/                      # 硬件/外部系统驱动
│  ├─ camera/                    # 相机驱动（USB 已实现）
│  ├─ io/                        # IO/Modbus 驱动（预留）
│  └─ mes/                       # MES 驱动（预留）
├─ app/                          # 应用主体
│  ├─ main_window.py             # 主窗口
│  ├─ status_bar.py              # 底部状态栏
│  ├─ help_dialog.py             # 帮助与激活对话框
│  ├─ standalone.py              # 页面独立运行工具
│  ├─ theme.py                   # 暗黑主题 QSS
│  ├─ checkdog/                  # 授权模块（独立）
│  │  └─ license_manager.py
│  ├─ pages/                     # 9 个功能页面
│  ├─ widgets/                   # 可复用 UI 组件
│  └─ services/                  # 配置、看板数据服务
├─ ui/                           # Qt Designer 基础 .ui 文件
├─ oriui/                        # 更贴近暗黑效果的 .ui 文件
├─ build/                        # 打包中间产物（生成）
└─ dist/                         # 打包输出（生成）
```

各目录职责：

| 目录 | 职责 |
| --- | --- |
| `app/pages` | 各功能页面，统一继承 `BasePage` |
| `app/widgets` | 可复用组件：相机图像、ROI 画布、ROI 编辑、统计卡片 |
| `app/services` | 页面级服务：配置读写、看板模拟数据 |
| `app/checkdog` | 软件授权、机器绑定、到期控制，独立于业务代码 |
| `drivers` | 相机、IO、MES 等硬件/外部系统驱动 |
| `algorithms` | 检测算法、图像处理算法（预留） |
| `config` | 页面参数 YAML |
| `flow` | 流程模板，按模板名作为目录，目录内保存 `template.yaml` |
| `ui` / `oriui` | Qt Designer 设计参考文件 |

---

## 4. 启动流程

1. `main.py` 创建 `QApplication`，设置应用名 `SOP Demo`、组织名 `SOP`。
2. 应用全局暗黑主题 `APP_STYLESHEET`（来自 `app/theme.py`）。
3. 创建并显示 `MainWindow`。
4. `MainWindow` 初始化底部状态栏、授权管理器、页面栈、导航栏。
5. 默认激活第一个页面（相机管理）。
6. 初始化授权检查，若已到期则启动 30 分钟宽限定时器。

---

## 5. 主窗口结构

文件：`app/main_window.py`

### 5.1 顶部导航栏

- 左侧 `SOP` 标题。
- 9 个可切换导航按钮，对应 9 个功能页面，使用 `QButtonGroup` 互斥。
- 右侧为全局操作提示标签 + “帮助”按钮。
- 页面顶部的 `tip_changed` 信号会同步到导航栏右侧的全局提示。

### 5.2 页面注册

页面通过 `NAV_ITEMS` 列表注册：

```python
NAV_ITEMS = [
    ("相机管理", CameraPage),
    ("运行看板", RunDashboardPage),
    ("流程编辑", FlowPage),
    ("硬件配置", HardwareConfigPage),
    ("结果查询", ResultQueryPage),
    ("MES对接", MesPage),
    ("日志", LogPage),
    ("参数", ParametersPage),
    ("人员管理", PersonnelPage),
]
```

新增页面时：

1. 在 `app/pages/` 下创建页面类，继承 `BasePage`。
2. 在 `app/pages/__init__.py` 中导出。
3. 在 `app/main_window.py` 的 `NAV_ITEMS` 中注册。

### 5.3 底部状态栏

文件：`app/status_bar.py`

- VSCode 风格，高度 28px。
- 左侧状态项：`ready`、`fps`、`image_size`、`hardware`、`camera`、`page`。
- 右侧状态项：`user`、`template`、`clock`，以及主窗口动态加入的 `dashboard_runtime`、`dashboard_shift`、`dashboard_cycle`、`dashboard_device`。
- 状态颜色支持：`info`、`ok`、`warn`、`error`。
- 右上角显示实时时钟。

更新方式：

```python
self.status_bar.set_status(key, text, kind)
```

### 5.4 信号联动

- `CameraPage.camera_metrics_changed` → 更新状态栏 FPS 和图像尺寸。
- `RunDashboardPage.dashboard_full_status_changed` → 更新状态栏运行时长、当班产量、平均节拍、设备状态。
- `RunDashboardPage.template_changed` → 更新状态栏当前模板。
- 页面 `tip_changed` → 更新导航栏右侧全局提示。

---

## 6. 页面公共基类

文件：`app/pages/base_page.py`

所有页面继承 `BasePage`，统一提供：

- `set_result(text)`：设置“检测结果”文案。
- `set_tip(text)`：设置“操作提示”文案，并发出 `tip_changed` 信号。
- `current_tip()`：返回当前提示。
- `add_to_content(widget, stretch)` / `add_layout_to_content(layout, stretch)`：向页面内容区添加控件。
- `auto_save_config()`：页面关闭前自动保存配置，默认空实现，由具体页面覆盖。
- `closeEvent()`：关闭前调用 `auto_save_config()`。

---

## 7. 九个功能页面详解

### 7.1 相机管理页（CameraPage）

文件：`app/pages/camera_page.py`

功能：

- 选择相机类型：USB 相机 / GIGE 相机。
- 选择驱动：DirectShow / Basler Pylon / HIKVISION SDK / GigE Vision。
- 枚举相机：USB 使用 OpenCV 真实枚举，GIGE 使用模拟数据。
- 打开/关闭相机、开始/停止预览、拍照。
- 打开本地图像。
- 相机参数：曝光、增益、帧率、触发模式、像素格式。
- 十字线居中。
- 保存相机配置到 `config/camera.yaml`。

实现状态：

- USB 相机已经通过 `drivers/camera/usb_camera.py` 真实接入 OpenCV。
- GIGE 相机为 demo，需在 `_enumerate_cameras()`、`_toggle_camera()` 等“插入点”替换为真实 SDK。
- 相机参数目前仅保存/回显，尚未真正下发到相机。

### 7.2 运行看板页（RunDashboardPage）

文件：`app/pages/run_dashboard_page.py`

功能：

- 顶部看板统计：当前模板 + OK / NG / 日 / 周 / 月 / 合格率统计卡片。
- 左侧相机画面显示区，绘制中心十字线和 ROI 框。
- 右侧流程步骤状态表，含步骤名称、ROI/标签、状态、耗时、缩略图。
- “复位故障”和“跳过当前步骤”按钮，跳步时弹窗选择 OK 通过或 NG 通过。
- 切换模板后更新看板数据和流程步骤。

实现状态：

- 数据来自 `app/services/dashboard_service.py` 的模拟数据，后续可替换为数据库、Redis 或 MES 实时统计。
- 相机画面区目前绘制占位图，尚未接入真实取流。

### 7.3 流程编辑页（FlowPage）

文件：`app/pages/flow_page.py`（项目中最复杂页面，约 660 行）

功能：

- 产品模板管理：新建、编辑、删除、保存当前模板。
- ROI 配置：画布预览 + 弹窗大图编辑（`RoiEditorDialog`），支持新增/删除/修改 ROI 的名称、X/Y/宽/高。
- 模型与标签：加载模型文件、加载标签文件、清空标签。
- 检测流程配置：步骤名称、使用 ROI、使用标签、置信度、检测数量、是否使用手势、手势姿势、是否启用。
- 步骤操作：添加、删除、上移、下移、单次执行。
- “隐藏模板/标签栏”可折叠左侧面板。
- 模板保存到 `flow/<模板名>/template.yaml`。

数据结构（模板 YAML）：

```yaml
name: 默认产品A
model_file: ''
labels:
- person
- helmet
- glove
- mask
- defect
rois:
- name: ROI-1
  x: 50
  y: 45
  w: 200
  h: 150
steps:
- name: 第一次检测
  roi: ROI-1
  label: person
  confidence: 0.55
  detection_count: 20
  use_gesture: false
  gesture: 无
  enabled: true
```

注意事项：

- 当前 `FlowPage` 的标签表为单列英文标签（列名“标签名称”）。
- 代码中仍保留未使用的 `_build_template_area()` 和 `_build_label_area()` 两个旧版构建方法（其中包含“英文/中文标签映射”两列布局），升级时应注意避免与新界面逻辑混淆。
- `flow/123/template.yaml` 是历史遗留模板，其 `labels` 为 `[英文, 中文]` 二元组结构，与当前默认模板的纯字符串标签结构不一致。
- “单次执行”目前只是弹窗选择 OK/NG 并给当前行上色，尚未调用真实算法。

### 7.4 硬件配置页（HardwareConfigPage）

文件：`app/pages/hardware_config_page.py`

功能：

- 串口设置：选择串口、打开/关闭串口；波特率、数据位、停止位、校验位横向排列。
- ModbusTCP 设置：IP 地址、端口、连接/断开。
- IO 控制：16 路输入状态指示灯 + 16 路输出开关按钮。
- 保存硬件配置到 `config/hardware.yaml`。

实现状态：

- 串口打开/关闭为模拟逻辑，真实串口库需在“插入点”接入。
- ModbusTCP 连接、输入读取、输出写线圈为模拟逻辑，需接入真实 Modbus 驱动。
- 输出按钮在 Modbus 未连接时为禁用状态。

### 7.5 结果查询页（ResultQueryPage）

文件：`app/pages/result_query_page.py`

功能：

- 查询条件：开始时间、结束时间、产品/模板、完成状态、报警状态。
- 操作按钮：查询、重置、导出 CSV、保存条件。
- 检测记录表：检测时间、产品/模板、批次号、完成状态、检测结果、提示信息、报警信息。
- 选中记录后，下方详情框显示完整信息。
- 汇总：总数、完成数、报警数。

实现状态：

- 数据为演示数据，尚未接入数据库或文件检索。
- 导出 CSV 为占位功能。

### 7.6 MES 对接页（MesPage）

文件：`app/pages/mes_page.py`

功能：

- MES 服务配置：上报地址、API Key。
- 测试连接（demo 模拟）。
- 选择上报报文：设备状态、检测结果、报警信息、统计信息、操作日志、自定义报文。
- 自定义报文模板编辑区。
- 发送测试报文、重置模板、保存配置。
- 状态输出框显示操作结果。

实现状态：

- 全部为 demo/占位，真实 MES HTTP/WebSocket 客户端需在“插入点”接入。

### 7.7 日志页（LogPage）

文件：`app/pages/log_page.py`

功能：

- 按级别筛选：全部 / DEBUG / INFO / WARN / ERROR。
- 添加测试日志、清空日志、导出 CSV、保存筛选。
- 日志表：时间、级别、来源、消息，不同级别显示不同颜色。

实现状态：

- 数据为演示日志，真实日志源（文件、数据库、远程日志服务）尚未接入。

### 7.8 参数页（ParametersPage）

文件：`app/pages/parameters_page.py`

功能：

- 系统参数：界面语言、日志级别、启用自动保存。
- 存储路径：检测结果目录、数据存储目录。
- 保存到 `config/system.yaml`。

说明：相机参数已迁移到相机管理页，检测参数已迁移到流程编辑页。

### 7.9 人员管理页（PersonnelPage）

文件：`app/pages/personnel_page.py`

功能：

- 人员列表：工号、姓名、角色、权限、状态。
- 新增、编辑、删除、刷新。
- 当前为演示数据，新增/编辑为占位弹窗，删除/刷新可用。

实现状态：可替换为数据库、LDAP 或权限配置。

---

## 8. 服务层

### 8.1 ConfigService

文件：`app/services/config_service.py`

统一配置读写服务：

- 页面配置：`config/<页面名>.yaml`。
- 流程模板：`flow/<模板名>/template.yaml`。
- 打包后（`sys.frozen`）根目录指向 exe 同级目录，保证可读可写。
- PyInstaller 打包后自动把内置 `config/`、`flow/` 初始文件复制到 exe 同级目录（通过 `_MEIPASS`）。

主要方法：

```python
save_page_config(page_name, data) -> Path
load_page_config(page_name) -> dict
save_template(template_name, data) -> Path
load_template(template_name) -> dict
```

### 8.2 DashboardService

文件：`app/services/dashboard_service.py`

运行看板模拟数据服务，包含：

- `DashboardStep`：步骤名称、ROI、标签、状态、耗时。
- `DashboardSnapshot`：模板名、OK/NG、日/周/月、合格率、步骤列表、ROI、运行时长、当班产量、平均节拍、设备状态。
- `TEMPLATES`：两个内置模拟模板（默认产品A、产品B）。
- `snapshot(template_name)`：生成快照。
- `refresh(template_name)`：模拟刷新一次统计。

后续可替换为数据库查询、Redis 缓存或 MES 实时统计接口。

---

## 9. 可复用组件（widgets）

| 文件 | 类 | 用途 |
| --- | --- | --- |
| `camera_image_view.py` | `CameraImageView` | 相机图像显示，支持滚轮缩放、右键拖动平移、十字虚线拖动、双击适配窗口 |
| `camera_view.py` | `CameraViewWidget` | 运行看板相机画面区，绘制中心线和 ROI 框 |
| `roi_canvas.py` | `RoiCanvas` | ROI 画布，绘制、选中 ROI，双击新建 |
| `roi_editor.py` | `RoiEditorDialog` | ROI 大图编辑弹窗，含表格编辑 |
| `stat_card.py` | `StatCard` | 看板统计卡片 |

---

## 10. 驱动层

### 10.1 相机驱动

文件：`drivers/camera/usb_camera.py`

- `UsbCameraDriver`：基于 OpenCV 的 USB 相机枚举、打开、关闭、取帧。
- `UsbCameraCaptureThread`：独立 QThread 采集线程，通过 `frame_ready` 信号把帧发送到 Qt 主线程。

GIGE 相机尚未实现，推荐目录 `drivers/camera/`，参考统一接口：

```python
class CameraDriver:
    def enumerate_devices(self) -> list[dict]: ...
    def open(self, device_id: str) -> bool: ...
    def close(self) -> None: ...
    def start_preview(self) -> None: ...
    def stop_preview(self) -> None: ...
    def capture(self) -> object: ...
    def get_frame(self) -> object: ...
```

### 10.2 IO 驱动

目录：`drivers/io/`（当前仅有 README）

推荐接口：

```python
class IODriver:
    def connect(self, ip, port) -> bool: ...
    def disconnect(self) -> None: ...
    def read_inputs(self, count) -> list[bool]: ...
    def write_coil(self, index, value) -> None: ...
```

接入位置：`app/pages/hardware_config_page.py`。

### 10.3 MES 驱动

目录：`drivers/mes/`（当前仅有 README）

推荐接口：

```python
class MesDriver:
    def configure(self, url, api_key) -> None: ...
    def test_connection(self) -> bool: ...
    def send(self, message_type, payload) -> bool: ...
```

接入位置：`app/pages/mes_page.py`。

---

## 11. 算法层

目录：`algorithms/`（当前仅有 README，无实际实现）

推荐统一检测接口：

```python
class DetectionAlgorithm:
    def load_model(self, model_path) -> None: ...
    def detect(self, image, rois, params) -> list[dict]: ...
```

接入位置：

- 流程编辑页“单次执行”。
- 运行看板正式检测流程。

---

## 12. 软件授权模块（checkdog）

目录：`app/checkdog/`

独立于业务页面代码，负责授权、机器绑定、到期控制。

### 12.1 LicenseManager

文件：`app/checkdog/license_manager.py`

核心逻辑：

- `machine_code()`：基于 `platform.system()` + `platform.node()` + `uuid.getnode()` 生成 SHA-256 摘要，格式化为 `XXXX-XXXX-XXXX-XXXX`。
- `generate_key(machine_code, start_date, end_date)`：生成动态密钥。
- `activate(key)`：解码并校验密钥签名、机器码、起止日期，成功后保存激活信息。
- `is_activated()` / `is_expired()` / `expiry_date()`：状态查询。
- 数据保存位置：
  - 开发环境：项目根目录 `license_data.json`。
  - 打包环境：exe 同级目录 `license_data.json`。

当前 `license_data.json` 为已激活示例，到期日期 `2027-08-28`。

### 12.2 授权码生成工具

文件：`license_tool.py`

独立 GUI 工具，用于输入机器码、起始日期、停止日期并生成密钥。

```powershell
python license_tool.py
```

### 12.3 到期控制逻辑（主窗口）

- 启动时若授权已到期，启动 30 分钟宽限定时器。
- 30 分钟后禁用页面栈，弹出“授权已到期”提示。
- 重新正确输入密钥后解锁。
- 帮助对话框“软件激活”页显示机器码、到期时间，支持输入密钥激活。

---

## 13. 配置与流程数据

### 13.1 config 目录

运行时各页面配置保存位置：

| 文件 | 对应页面/内容 |
| --- | --- |
| `camera.yaml` | 相机类型、驱动、曝光、增益、帧率、触发、像素格式 |
| `hardware.yaml` | 串口、波特率、数据位、停止位、校验位、Modbus IP/端口 |
| `mes.yaml` | 上报地址、API Key、选中报文、自定义模板 |
| `system.yaml` | 语言、日志级别、自动保存、结果目录、数据目录 |
| `result_query.yaml` | 查询时间、产品、状态、报警筛选条件 |
| `log.yaml` | 日志级别筛选 |

### 13.2 flow 目录

流程模板以模板名作为目录，目录内保存 `template.yaml`：

```text
flow/
├─ 默认产品A/
│  └─ template.yaml
├─ 产品B/
│  └─ template.yaml
└─ 123/
   └─ template.yaml   # 历史遗留模板，标签为双语二元组结构
```

---

## 14. PyInstaller 打包

### 14.1 打包配置说明

`打包注释.md` 记录了常用 PyInstaller 参数：

- `-F`：单文件 exe。
- `-D`：目录模式（默认）。
- `-w` / `-c`：窗口程序 / 命令行程序。
- `--icon`：指定图标。
- `--name`：指定程序名。
- `--uac-admin`：请求管理员权限。
- `--add-data`：打包附带数据目录。

### 14.2 三个 spec 文件

| 文件 | 模式 | 特点 |
| --- | --- | --- |
| `main.spec` | 目录模式 | 默认 `main`，console 模式，无 datas |
| `SOP.spec` | 单文件模式 | `SOP`，附带 config/flow，windowed |
| `SOP_APP.spec` | 目录模式 | `SOP_APP`，附带 config/flow，windowed，uac_admin，图标 MyAppLog.ico |

README 中的打包命令：

```powershell
# 单文件 exe
pyinstaller --noconfirm --windowed --onefile --name SOP --add-data "config;config" --add-data "flow;flow" main.py

# 小 exe + 外部配置目录，运行时自动创建
pyinstaller -D main.py --noconfirm --windowed --icon=MyAppLog.ico --name SOP_APP --uac-admin --add-data "config;config" --add-data "flow;flow"
```

注意：Windows 下 `--add-data` 源与目标用分号 `;` 分隔，Linux/macOS 用冒号 `:`。

打包后 `ConfigService` 会把内置 `config/`、`flow/` 初始文件复制到 exe 同级目录，保证运行时可读写。

---

## 15. UI 设计文件

- `ui/`：Qt Designer 基础 `.ui` 文件，每个页面一份，共 10 个（含主窗口）。
- `oriui/`：更贴近当前暗黑显示效果的 `.ui` 文件，预置暗黑 QSS，用于更直观的预览。

包含文件：

```text
main_window.ui
camera_page.ui
run_dashboard_page.ui
flow_page.ui
hardware_config_page.ui
result_query_page.ui
mes_page.ui
log_page.ui
parameters_page.ui
personnel_page.ui
```

使用方式：

1. 用 Qt Designer 打开 `.ui` 文件二次编辑。
2. 用 `pyuic6` 生成 Python UI 代码：

```powershell
python -m PyQt6.uic.pyuic ui\camera_page.ui -o ui\camera_page_ui.py
```

注意：当前程序以 `app/pages/*.py` 手写 UI 为准，`.ui` 文件仅作为设计参考和交接使用。

---

## 16. 现有文档与历史变更记录

| 文件 | 内容 |
| --- | --- |
| `README.md` | 项目总体说明、运行、打包、已实现内容 |
| `PROJECT_ARCHITECTURE.md` | 目录结构、主要接口、扩展位置 |
| `打包注释.md` | PyInstaller 打包参数说明 |
| `app/扩展.md` | 历史需求/扩展记录（第 1 批） |
| `app/修改.md` | 历史修改记录（第 1 批） |
| `app/修改2.md` | 历史修改记录（第 2 批） |
| `app/修改3.md` | 授权模块相关修改记录 |

这些文件记录了项目早期多轮需求迭代过程，后续升级时可作为背景参考。

---

## 17. 当前实现状态总览

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 主窗口 / 导航 / 状态栏 | 已实现 | 暗黑主题，9 页面切换 |
| USB 相机采集 | 已实现 | OpenCV 枚举、打开、预览、拍照 |
| GIGE 相机 | Demo | 需接入真实 SDK |
| 运行看板 | Demo | 模拟数据，需接数据库/实时统计 |
| 流程编辑 | 框架已实现 | 模板/ROI/步骤可编辑保存，算法未接 |
| 检测算法 | 预留 | `algorithms/` 暂无实现 |
| 硬件配置 | Demo | 串口、ModbusTCP、IO 均为模拟 |
| 结果查询 | Demo | 演示数据，导出为占位 |
| MES 对接 | Demo | 需接入真实 MES 客户端 |
| 日志 | Demo | 演示数据，未接真实日志源 |
| 参数 / 人员管理 | 框架已实现 | 参数可保存，人员为演示数据 |
| 软件授权 | 已实现 | 机器绑定、密钥激活、到期控制 |

---

## 18. 升级与扩展注意事项

1. **新增页面**：创建继承 `BasePage` 的类 → 在 `app/pages/__init__.py` 导出 → 在 `main_window.py` 的 `NAV_ITEMS` 注册。
2. **接入真实硬件/外部系统**：优先在对应 `drivers/` 目录实现统一接口，再在页面 `插入点` 注释处替换 demo 逻辑。
3. **新增算法**：在 `algorithms/` 下实现统一 `detect()` 接口，供流程编辑“单次执行”和运行看板调用。
4. **配置文件**：新增页面配置通过 `ConfigService.save_page_config / load_page_config` 读写，自动落到 `config/<页面名>.yaml`。
5. **流程模板**：通过 `ConfigService.save_template / load_template` 读写，落到 `flow/<模板名>/template.yaml`。
6. **UI 二次设计**：修改 `ui/` 或 `oriui/` 的 `.ui` 后，需同步 `app/` 手写 UI；若修改了界面，记得同步维护两份 UI 文件。
7. **打包**：新增需打包的数据目录时，需同步更新 `.spec` 文件的 `datas` 配置。
8. **授权模块**：授权逻辑集中在 `app/checkdog/`，业务代码不要与之混在一起。
9. **历史遗留**：`flow/123/template.yaml` 的标签结构、`FlowPage` 中未使用的旧方法可能与当前逻辑不一致，修改前需先确认当前生效代码路径。
10. **搜索插入点**：代码中多处用 `插入点` 注释标记了可扩展/可替换位置，升级时可直接搜索该关键词快速定位。

---

## 19. 快速定位索引

| 想做的事 | 关键文件 / 位置 |
| --- | --- |
| 修改主窗口、导航、页面注册 | `app/main_window.py` |
| 修改全局主题/样式 | `app/theme.py` |
| 修改底部状态栏 | `app/status_bar.py` |
| 修改帮助/激活 | `app/help_dialog.py` |
| 修改相机逻辑 | `app/pages/camera_page.py`、`drivers/camera/usb_camera.py` |
| 修改看板 | `app/pages/run_dashboard_page.py`、`app/services/dashboard_service.py` |
| 修改流程编辑 | `app/pages/flow_page.py`、`app/widgets/roi_canvas.py`、`app/widgets/roi_editor.py` |
| 修改硬件/IO | `app/pages/hardware_config_page.py`、`drivers/io/` |
| 修改结果查询 | `app/pages/result_query_page.py` |
| 修改 MES | `app/pages/mes_page.py`、`drivers/mes/` |
| 修改日志 | `app/pages/log_page.py` |
| 修改参数 | `app/pages/parameters_page.py` |
| 修改人员管理 | `app/pages/personnel_page.py` |
| 修改授权 | `app/checkdog/license_manager.py`、`license_tool.py` |
| 配置读写 | `app/services/config_service.py` |
| 打包配置 | `SOP_APP.spec`、`SOP.spec`、`main.spec` |
| 设计参考 UI | `ui/`、`oriui/` |
